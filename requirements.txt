anthropic>=0.40.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
langchain-chroma>=0.1.0
chromadb>=0.5.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
rich>=13.9.0
httpx>=0.27.0
tiktoken>=0.8.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
# CP0 — delta tracking
gitpython>=3.1

# CP1 — AST-aware chunking
tree-sitter>=0.22.0
tree-sitter-python>=0.22.0
tree-sitter-javascript>=0.22.0
tree-sitter-typescript>=0.22.0

# CP1 — graph database (embedded, no server needed)
# NOTE: kuzu requires Python 3.11 or 3.12 — no wheel for 3.13+ yet.
# If you are on Python 3.13+, the graph features are skipped gracefully.
# To enable: recreate the venv with Python 3.11 → then pip install kuzu>=0.6.0
kuzu>=0.6.0; python_version < "3.13"

# CP2 — testing
pytest>=8.0.0
